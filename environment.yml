name: xcube-clms
channels:
  - conda-forge
dependencies:
  # Required
  - python>=3.10
  - xarray
  - xcube >= 1.7.0
  - cryptography
  - tqdm
  - ipywidgets
  # for testing
  - black
  - flake8
  - pytest
  - pytest-cov
  - pytest-recording
