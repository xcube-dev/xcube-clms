name: xcube-clms
channels:
  - conda-forge
dependencies:
  # Required
  - python>=3.10
  - xarray
  - xcube >= 1.7.1
  - cryptography
  - tqdm
  - ipywidgets
  - requests
  - pyproj
  - fsspec
  # for testing
  - numpy
  - black
  - flake8
  - pytest
  - pytest-cov
  - pytest-recording
